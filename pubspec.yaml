name: async_notifier
description: A ValueNotifier extension for all async states. Listen, notify, and manage loading, error and data in one place.
version: 0.1.0
homepage: https://www.branvier.com

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

